# Global Bun packages installed via `dot bun add -g NAME[@VERSION]` and `dot bun remove NAME`
# Omit the version to always track the latest release.
@biomejs/biome
@earendil-works/pi-coding-agent
cloudflared
frog
mcp-remote
wrangler
